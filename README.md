# mini-lang

todo:
- create scanner method for converting tokens array from infix to postfix

- example 1:
  - infix: [a, +, b, *, c, + d]
  - postfix: [a, b, +, c *, d, +]

- example 2:
  - infix: [print, (, x, )]
  - postfix: [x, print]

